# Dependencias exclusivas del entorno de pruebas.
# Se mantienen aparte de requirements.txt para que el despliegue en Render
# no instale herramientas que no necesita en producción.
#
#   pip install -r requirements.txt -r requirements-test.txt

pytest>=8.0
pytest-cov>=5.0
httpx>=0.27.0
